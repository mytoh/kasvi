

(install
  ( (run-process `(gauche-install -T ,(build-path ( *lehti-dist-directory* ) "kasvi")
                                  "bin/kasvi")
                 :wait #t)
   ;; library sources
   (copy-directory* (build-path ( *lehti-cache-directory* ) "kasvi/lib")
                    (build-path ( *lehti-dist-directory* ) "kasvi/lib"))
   )
  )
